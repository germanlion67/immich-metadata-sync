version: '3.8'

services:
  immich-metadata-sync:
    image: germanlion67/immich-metadata-sync:latest
    container_name: immich-metadata-sync-test
    volumes:
      - /home/immich/immich-library/library:/app/library
      - ./logs:/app/logs
    # Falls du keine stack.env hast, kommentiere die nächste Zeile mit # aus
    env_file:
      - stack.env
    healthcheck:
      test: ["CMD", "python", "/app/healthcheck.py"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 5s
    # Das Docker Image enthält bereits alle Dependencies und Scripts
    # Für andere Befehle, überschreibe das CMD:
    # command: ["python", "/app/immich-ultra-sync.py", "--all", "--only-new"]
